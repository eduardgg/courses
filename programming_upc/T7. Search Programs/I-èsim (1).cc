#include <iostream>
#include <cstring>
using namespace std;

int main() {
	int n,m,k;
	cin >> n;
	for (int i=1; i<n; ++i) cin >> m;
	cin >> k;
	cout << "A la posicio " << n << " hi ha un " << k << "." << endl;
}
  
	
